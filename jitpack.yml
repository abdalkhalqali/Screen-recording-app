jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.2-tem || true
  - sdk use java 17.0.2-tem || true

install:
  - ./gradlew qa-snap-sdk:assembleRelease qa-snap-sdk:publishToMavenLocal

# Skip the default install step
skip_install: true